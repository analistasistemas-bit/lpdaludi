# Task 1: Atualização da Navbar e Metadados Executivos

## Files:
- Modify: `index.html`

## Global Constraints:
- Arquivo único de entrega: todo código vive exclusivamente em `/Users/diego/Desktop/IA/LandingPage_Daludi/index.html`.
- Paleta oficial: `--bg-primary: #050505`, `--bg-secondary: #0B0F14`, `--bg-card: #11151A`, `--hero: #00F5C4`, `--text-primary: #E6F1F0`, `--text-secondary: #8FA3A1`, `--border: #1F2A33`.
- Emojis proibidos nos títulos e cards de serviço/dor — utilizar ícones vetoriais SVG minimalistas inline.
- PubliAI (`app.publiai.daludi.com.br`) deve ser destacado com fidelidade à sua operação real.
- Contatos reais preservados: WhatsApp `5581996459595`, LinkedIn Diego Araújo, formulário de contato.
- Não utilizar MCP insforge para deploy automático.
- Não faça commit com `git push`, apenas commits locais organizados se necessário ou aguarde o final.

## Objetivo específico:
1. Atualizar o `<title>` e meta description em `index.html`:
   - Title: `DALUDI | Engenharia de Software, IA e Gestão de Marketplace`
   - Description: `Arquitetura de Sistemas, Inteligência Artificial e Produtos Digitais em Produção. Conheça a operação do PubliAI no Mercado Livre e nossas soluções de IA para empresas.`
2. Atualizar a Navbar (Desktop e Mobile Drawer):
   - Inserir o link para `#publiai` com badge `NOVO` ou `APP` ao lado de Soluções.
   - Ordem limpa de links:
     - Soluções (`#solucoes`)
     - PubliAI (`#publiai` com badge sutil de produto)
     - Autoridade (`#fundador`)
     - Cases (`#cases`)
     - Método (`#metodo`)
     - Botão CTA: `Falar com Diego` (`#contato`)
3. Garantir que o link do PubliAI no menu mobile drawer também esteja presente e funcionando perfeitamente.
4. Manter o estilo e a responsividade intactos, sem quebras de layout.
